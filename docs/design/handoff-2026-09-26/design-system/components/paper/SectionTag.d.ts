import * as React from 'react';
/** Yellow section label over a lead or article headline. */
export interface SectionTagProps { children: React.ReactNode }
export function SectionTag(props: SectionTagProps): JSX.Element;
