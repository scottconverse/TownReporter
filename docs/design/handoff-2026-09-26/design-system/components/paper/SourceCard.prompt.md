One record in an article’s “How we reported this” section.

```jsx
<SourceCard role="Announcing source" title="Oct. 8 funding hearing packet" host="longmont.primegov.com" captured="Sep 25, 2026, 7:00 PM" currentHref="…" capturedHref="/evidence/2504" compareHref="/evidence/compare?url=…" />
```

**Props and rules**
- Place in a 2-column grid (1 on phone) with 1px gaps over `var(--grid)`
- `compareHref` only when more than one capture exists
