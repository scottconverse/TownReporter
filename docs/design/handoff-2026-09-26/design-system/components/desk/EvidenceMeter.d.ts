/** Records opened (filled) vs could not open (red outline), with words. */
export interface EvidenceMeterProps { opened: number; failed?: number }
export function EvidenceMeter(props: EvidenceMeterProps): JSX.Element;
