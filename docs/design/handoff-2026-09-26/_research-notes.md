# Repo facts gathered (scottconverse/townreporter @ main, 0.6.68, 2026-09-26)

Stack: TanStack Start (React 19, TanStack Router file routes in src/routes, React Query), Vite 8, Tailwind 4 + bespoke CSS (src/styles.css 91KB, src/desk-astra.css, src/reader-astra.css, src/fonts.css), Radix UI primitives, lucide-react, recharts (already a dependency), sonner toasts, kysely + pg / PGLite, better-auth, zod. Node >= 22. Tests: node --test, Playwright e2e scripts (test:flows, test:opinion...), lint max-warnings 0, typecheck.

Routes present: index, articles.$slug, corrections, about, how-we-report, evidence.$versionId, evidence.compare, feed, login, desk (gate), desk.index (Command Center), desk.queue, desk.story.$leadId (116KB), desk.story.draft.$draftId, desk.import (document intake), desk.opinion, desk.published, desk.sources, desk.scan, desk.dark (79KB), desk.follow-ups, desk.legal-removals, desk.memory, desk.ops (Server), desk.stats, desk.setup.

Desk chrome: src/components/desk-chrome.tsx exports DeskShell, InkButton (tones: solid/quiet/danger/ghost, small), Chip, SecHead; nav array NAV lines 48-54: Sources, Scan, Queue, Published, Opinion, Server(/desk/ops), Stats; secondary: Dark Desk, Follow-ups, Beat memory; plus /desk/import. Notice in src/components/states.tsx (also ScreenError, ListSkeleton, ScreenPending, EmptyState). announceToDesk() in desk-chrome-utils.

Components already present: model-picker.tsx, custom-ai-connections.tsx, daily-scan-settings.tsx, dark-dials-panel.tsx, desk-leads.tsx, desk-name-check.tsx, draft-batch-result.tsx, draft-reconcile-control, draft-scope-picker, finding-evidence-review.tsx, follow-up-item.tsx, investigation-brief.tsx, meeting-capture-settings, meeting-source-block, meetings-activity, named-outlets-setup, page-watch-panel.tsx, paper-chrome.tsx, paper-setup-form, provenance.tsx, provider-signin-button, provider-time-field, reader-controls.tsx, routine-notice-*, correction-form.tsx, copy-button, ai-disclosure.

Jobs: desk_jobs table (0013): kind, subject_id, status (queued...), stage text, error, result_json, started/finished; heartbeat + claim_token (0017, 0033); failover note (0032). UI polls: story page 2s while waiting, desk.index 5s, scan/queue/dark conditional; dark_runs has usage + stop_reason for "live progress" (0059). => Progress infra EXISTS (stage string + heartbeat + polling). New: structured stage index/total, step text, pct, last_activity exposure, stall threshold in UI, cancel.

Models: src/lib/news/provider-registry.ts is the single registry. Picker ids: codex-astra, codex-frontier (label "Codex Sol"), codex-balanced ("Codex Terra"), codex-luna, claude-fable, claude-frontier ("Claude Opus"), claude-sonnet, claude-haiku, local-model. Retired: grok-oauth (owner: "REMOVE Grok" - offered nowhere). Automatic ladder rungs: deepseek-flash (Ollama, rank1), qwen-local (LM Studio, rank2, requires loaded), codex-balanced (rank3). Custom connections: `custom:<uuid>` (custom_ai_connections table, OpenAI-compatible, e.g. Gemini). Surfaces: story, scan, opinion, dark, forced. Efforts: none/low/medium/high/xhigh/max, per-model lists (ASTRA, CODEX_56, CLAUDE_CLI, DEEPSEEK...). provider_settings table: per-paper call_ms/wall_ms/enabled + localModel pick. FORCED_FAILOVER_LADDER = codex-balanced, claude-sonnet. Rule: "Anywhere an AI does something, the editor must be able to pick the model." Rule: never load/unload a local model from the paper. local-models.ts discovery probes LM Studio :1234 and Ollama :11434 every 60s.
=> Models page "who does what" = extend surfaces to per-job keys + fallback list; much exists.

Follow-ups: follow_ups table (0042) manual: who, what, due_on, status open|answered|dropped, nudged_at, reply_text; /desk/follow-ups route + FollowUpItem (Record reply / Nudge / Drop). Owner now says: no humans; follow-ups must drive AI agents. => Rework: reuse manual page watch (0046: source_monitors.manual_watch, watch_reason, watch_state, watch_model_choice, manual_watch_checks, manual_watch_actions) as the engine for "re-check pages"; new "search" and "watch for agenda" agent kinds.

Page watch EXISTS (page-watch-panel.tsx, 0046, 0062). Legal removal EXISTS (0047 legal_removal 13KB, desk.legal-removals.tsx, legal-removal-types.ts). Configurable sections EXIST (0045 newsroom_sections with brief/instructions/visible/position/replacement_key). Daily scan EXISTS (0050 daily_scan_policies, pause_reason; 0065 coverage accounting; 0066 source packs). Draft batches (0051) with Redraft. Meeting capture (0067-0085) transcripts. Import provenance (0091). Editor headline control (0093). Opinion documents (0057/0058). Dark gates two-stage (0043), dark run budget (0059).

Stats: page_views(newsroom_id, target 'site'|'story:<slug>', day, count) raw beacon counts, no identity; desk.stats.tsx shows Today/7/30/all + per-story; saved reports JSON under TOWNREPORTER_DATA_ROOT/reports/stats/newsroom-ID/. recharts available.

Existing design rules (docs/design): WCAG AA both themes (scripts/contrast-audit.mjs), nothing informational <14px (scripts/desk-min-font.test.mjs), one button/chip/notice family, quiet styling never carries meaning alone, 44px targets. Desk dark rule in repo was pure black/white; OWNER OVERRIDE 2026-09-26: desk and paper use warm black #1b1916.

Current desk theme: src/desk-astra.css ("Astra") — light --bg #f6f7f8, --mut #637078, --line #dce2e5, --warn #a63e38; dark --bg #182024, --mut #a7b5ba, --line #39474d, --warn #f3a19b. Public paper: src/reader-astra.css + src/styles.css. Fonts in src/fonts.css + public/fonts (self-hosted). Redesign REPLACES both palettes with the locked tokens.
Stats page today also shows "Section chosen by hand" count and Saved reports (daily/weekly/monthly JSON via stats-reports) — KEEP both in new Stats.
src/lib/news/views.ts = getViewStatsFn + ensureViewsSchema; beacon fires after page render.
