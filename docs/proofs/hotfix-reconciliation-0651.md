# Production hotfix reconciliation — 2026-09-17

**Branch:** `fix/finish-townreporter-20260916` (local, unpushed)
**Source handoff:** `C:\Users\scott\.codex\attachments\bda1ffcb-f378-40d6-b521-d5f645099c34\pasted-text.txt`
**Scope:** reconcile Scott's 2026-09-17 one-off production hotfixes into the release candidate. The production checkout and database were not touched in this slice.

Each area carries exactly one outcome from the handoff's section 7: **already implemented / needed and incorporated / machine-data-only / no longer needed**.

## 1. Daily scan recovery — outcome: machine-data-only

The production policy row change (codex-terra to claude-sonnet, unpaused, revision 7) is a data change, not release code. Scott's decision: no product hard-coding of any default scan runtime; this row is machine/data-only and stays in installation/operator guidance, not product defaults.

Candidate-side code evidence (present in this branch; not changed by this slice):
- Failover-or-pause exists in source: `src/lib/news/daily-scan.server.ts` validates the configured runtime against the shared ladder and falls back to an allowed next runtime (`validateDailyRuntime`, `automaticFailoverReason`, `failoverNoteSentence`); when no runtime validates it pauses with an explicit reason (`update daily_scan_policies set paused=true,pause_reason=...`).
- Editor-facing editability: Server > Daily scan renders through `src/components/daily-scan-settings.tsx` (imported by `src/routes/desk.ops.tsx`), covering local time, model, effort, source cap, and selected sources.
- Focused tests exist in-tree: `src/lib/news/daily-scan.test.ts`, `daily-scan.execution.test.ts`, `daily-scan.postgres.test.ts`, `daily-scan.worker.test.ts`.

Limits: no scheduled-policy run was executed in this slice (no live provider calls authorized); a zero-lead successful scan remains a completed result by design, not a failure.

## 2a. Codex catalog `ollama-launch-models.json` provenance — outcome: machine-data-only (report-only)

Scott's decision: fixes stay in the TownReporter repo; external tooling under the user's `.codex` is a separate product and is not edited. Findings (report-only, from this session's bounded searches):
- The catalog is **not generated by TownReporter**: `git grep ollama-launch-models` in this repository returns no matches.
- It is consumed by Codex CLI configuration: `C:\Users\scott\.codex\config.toml` (line 24) sets `model_catalog_json = "C:\Users\scott\.codex\ollama-launch-models.json"`; the same reference appears in `ollama-deepseek.config.toml` and `routing-backups/20260916-082039/`.
- The exact generator was not located within scoped searches of `.codex`, the npm global tree, `%LOCALAPPDATA%\OpenAI`, and Ollama directories. Adjacent evidence: `C:\Users\scott\.codex\models_cache.json` carries `client_version: 0.154.0` (a Codex CLI catalog cache), and `C:\Users\scott\.codex\backups\ollama-picker-20260916-154649\` holds an earlier copy of both the catalog and `ollama-launch-codex-routing.json`, indicating a Codex-side picker/routing tool manages the file.
- Conclusion: machine/data-only, report-only per Scott's direction. TownReporter-side follow-up (open, not in this slice): tolerate a malformed catalog gracefully, translate CLI parse failures into actionable editor copy, and document the manual repair in operations guidance.

## 2b. provider_logins failed-login supersession — outcome: needed and incorporated

Commit **`5af1525`** implements the real behavior the production workaround (a manually inserted `done` row) approximated:
- `supersedeResolvedLogin()` in `src/lib/news/provider-login.server.ts`: when `providerStatuses()` observes the provider signed in, the latest login record is re-probed; a passing probe marks the stale `failed` row `done` with cleared detail. Historical rows are never deleted — the failed row remains reviewable in `provider_logins`.
- Focused regression: `src/lib/news/provider-login.test.ts`, "a successful signed-in probe supersedes an older failed-login record" (passed in this session).
- Evidence: the `5af1525` diff for `src/lib/news/provider-login.server.ts` and `src/lib/news/provider-login.test.ts`.

## 3. Gemini custom connection (gemini-2.5-flash to gemini-3.5-flash) — outcome: machine-data-only

The production model switch was a data-row change plus a live discovery/test cycle, not release code. Scott's product decision recorded here: **a paid API/model connection discloses its cost once, at setup** — not on every use, not in repeated banners. Product principle: a user unsure how to use an API or paid model probably should not be entering those credentials at all.

Open release-candidate work (explicitly not done in this slice; needs Scott authorization for live calls):
- Do not hard-code `gemini-3.5-flash` as a default; re-check Google's live catalog at release time.
- Improve safe error reporting to distinguish an unsupported/stale model from an unsupported endpoint (the generic 404 copy obscured that the key and endpoint worked).
- Verify `Discover models`, model selection, save/upsert (never exposing the raw unique-constraint error for a duplicate name), and `Test connection` through the interface.

## 4. Grok / SuperGrok OAuth — outcome: machine-data-only

Production recovery wrote the xAI credential through TownReporter's encrypted `XaiOauthCredentialStore` via a direct invocation of the installed `@earendil-works/pi-ai` provider. That is machine/credential state, not a code change.

Open release-candidate work (explicitly out of scope in this slice per the brief):
- Reproduce sign-in from the actual Server-page button in a clean state; the direct recovery path is not proof that the UI flow is reliable.
- Determine why the in-process UI login attempt failed immediately while a direct invocation of the same installed provider obtained a device code moments later (transient xAI availability is one hypothesis, not a conclusion).
- Verify the URL/code remains visible long enough, polling survives the page lifecycle, approval stores the encrypted credential, model discovery runs, selection persists, and `Test connection` passes.

## 5. Ollama cloud vs on-device labeling — outcome: needed and incorporated

Commit **`5af1525`** fixes `enrichOllama()` in `src/lib/news/local-models.ts`: the cloud detector now matches every documented naming shape — `:cloud`, `-cloud`, and versioned `x.y-cloud` — instead of only `:cloud`, so hosted models are no longer mislabeled on-device.
- Focused regression: `src/lib/news/local-models.test.ts`, "labels every Ollama cloud naming shape as cloud, not on-device," covering all three shapes plus a plain on-device tag; verified red against the old detector and green with the fix in this session.
- The picker's existing copy distinguishes hosted from on-device and keeps vision language precise (attached images and scanned pages; ordinary search and extracted text do not need it).

## Hygiene

- The scratch `work/` directory was removed from the repository in commit **`721e2ed`**; copies are preserved outside the repo under the session work directory. The committed tree contains only intentional files.

## Verification standard

Per the handoff's acceptance standard: nothing here is claimed complete from catalog listings, mocked provider tests alone, HTTP 200 from the homepage, or the existence of a button. Provider acceptance requires the actual editor-visible choice plus a bounded live response through TownReporter's production transport. Scheduled scan acceptance requires an actual completed scheduled-policy run. OAuth acceptance requires the real UI flow from disconnected state through encrypted persistence and a live model response. None of those live acceptances were run in this slice, and none are claimed.