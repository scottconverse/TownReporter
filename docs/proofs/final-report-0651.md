# TownReporter finishing directive — implementation report

> **Provenance note:** Fine-grained history was consolidated into a single squash commit before publication; commit hashes cited below refer to the pre-squash local history.

**Branch:** `fix/finish-townreporter-20260916` (local and unpushed; `git rev-list --count origin/main..HEAD` is the authoritative current distance)
**Base:** `c43273702d159b63a405b0053679d22b6b236149` (v0.6.51)
**Scope:** the eleven requirements in the finishing directive. Nothing below is a claim beyond the evidence named in its row.

**No publish, merge, tag, release, or deployment has been performed.** Production still serves 0.6.51; the promotion backup is untouched.

| Requirement | Status | Evidence | Remaining limit |
|---|---|---|---|
| 1. Promotion script recognises the real Windows server process and still refuses unrelated Node | Fixed, locally tested | `1a683b0`. Repro before the fix: the forward-slash predicate failed against the real backslash command line. `scripts/ops-promote-process.test.mjs` 1/1; `scripts/ops-scripts.test.mjs` 24/24; the live production PID matched the classifier | Not exercised by an actual promotion; that action needs Scott's explicit go-ahead |
| 2. Windows installation usable by a nontechnical publisher | **Packaged install verified on a clean data root**; fresh-machine acceptance still unverified | `20c14ee` and `docs/windows-install.md`: ZIP selection, install/start, owner setup and its no-reset boundary, paper configuration, writing prerequisites, URLs, data/log/backup locations, support guidance. Installer test 6/6. The data-folder inventory was verified against `installer/Common.ps1`, `Install.ps1`, and `Start.ps1`. The exact candidate ZIP was then extracted into a fresh folder and installed with a new data root on isolated ports 4388/15432: installer exit 0, Node 22.23.2 and PostgreSQL 17.11 provisioned, dependencies and Chromium installed, production build and database setup completed, homepage HTTP 200 with 0.6.51, and `/desk` HTTP 200. The installation then stopped cleanly with persistent data retained. A later Playwright walk restarted that packaged server and its bundled PostgreSQL, created the first owner, saved `Packaged Ledger` / Longmont / Colorado setup, landed on `/desk` with the configured paper name, and recorded no browser errors. See [the clean-data-root proof](windows-host-install-0651.md) and [the packaged first-run proof](windows-packaged-first-run-0651.md) | A clean Windows Sandbox attempt on 2026-09-17 reached the real installer but failed during dependency download with repeated `Invoke-WebRequest` “Unable to connect to the remote server” errors; no install result was produced. See [the sandbox attempt](windows-sandbox-attempt-0651.md). **No fresh-machine human acceptance was performed**, so none is claimed. The clean-data-root install is not a fresh-OS or human first-run test |
| 3. First-run city/newsroom configuration complete and understandable | Verified in an isolated browser walk | `d1c6833` plus `docs/proofs/route-and-first-run-0651.md`. Fresh in-memory newsroom: first account owns the desk; setup saves and lands on `/desk`; Paper/Sections/Sources/Scan/Server screens walked. Live isolated reads confirmed the Daily scan panel (local time, timezone, shared model picker, effort, daily source limit, accepted sources, disabled schedule state), the Newspaper sections panel (display name, permanent key, nav visibility, brief, scan instructions, assigned sources, retirement, two-phase save), and the Sources screen (add/import/upload, tier rule, counters, empty state) | In-memory database and a fake Codex CLI; no real provider was exercised in the first-run walk |
| 4. Model selection and technical-failure recovery consistent everywhere | Registry consistency and live named-provider path verified; failover invariants covered by focused tests | `ac1c005` aligned OCR Automatic with the shared ladder. Inventory across Story, Opinion, Queue, Dark Desk, Scan, evidence, OCR, custom connections, and Server surfaces; OCR 34/34, picker 16/16, model-choice + registry 50/50. Failover invariants: 85 passing tests across six suites, including terminal refusals and technical-only unfinished-call rerouting. A bounded live run completed an explicit **Claude Sonnet** supplied-material Story draft on the isolated build with no switch; see [the second-provider proof](live-second-provider-0651.md). A separate live run requested an intentionally unreachable Custom API connection and completed on **Codex Terra**; the workspace recorded `Model note: This draft moved to Codex Terra because Custom API connection was unavailable`; see [the live failover proof](live-failover-0651.md). The `provider: "injected"` verification label was fixed in `5ff51b0` and confirmed live in run 8 | One forced connection-unavailable failover; this does not prove every mid-call failure class or provider pair |
| 5. Queue batch drafting and redrafting repaired and proven | Verified at unit and render level | `41fa450`. RED before the fix: a restored batch lost its persisted `modelChoice`/`modelEffort`, stranding Redraft. `draft-batch.test.ts` 24/24, `draft-batch-result-render.test.mjs` 4/4, worker/compat 10/10, typecheck clean. The live isolated Queue surface confirmed the named-runtime picker, the no-publish boundary, and the per-row **Include in batch draft** checkbox; `scripts/lead-badge-render.test.mjs` 15/15 covers the row relationship and suggested-focus behavior | No full browser batch-draft run this session; the redraft path is covered by focused and render tests |
| 6. Document ingestion and OCR honest, resilient, usable | Verified focused plus live image-only OCR | `3ba690f`. `story-documents.test.ts` 7/7 — including a new 37-page fixture regression proving one failed-extraction retry reads at most one 12-page batch (12/37 then 24/37, checkpoint retained, still `failed` with the documented retry detail) before a later retry finishes pages 25-37 without rereading. The bounded-retry repair in `3ba690f` replaced the old unbounded `while` retry loop: one retry consumes at most one 12-page batch, plus one final extraction when the remainder is smaller than a batch. RED-vs-GREEN: with the old loop restored, the new regression fails (one retry consumed all 37 pages in one request). Earlier `a6fcc04` evidence: `story-documents.test.ts` covered a 15-page PDF resuming at page 13 without rereading; migration `0063` applied; honest read/pending/skipped/failed/retry states. A live one-page image-only PDF uploaded through the isolated Desk composer was recorded as **read · 1/1 parts read**, with 208 extracted characters retained and no console errors ([image-only OCR proof](image-pdf-ocr-live-0651.md)). A **real** published 258-page Longmont council packet was then uploaded through the same real picker: the product read one bounded 12-page batch, reported `Read 12 of 258 pages; completed pages retained. Retry to continue.`, durably checkpointed those 12 pages and retained 18,080 characters without claiming a full read ([real council packet proof](real-council-packet-ingestion-0651.md)). Pages 1-2 of that same real packet were rasterized into a genuine image-only PDF and read by the vision path as `1/1 parts read`, 2 pages, 2,844 characters, matching 13/13 ground-truth probes ([real-content OCR proof](real-content-image-pdf-ocr-0651.md)) | OCR was exercised on real civic content (pages 1-2 of the published Longmont council packet, rasterized to an image-only PDF, 2/2 pages, 2,844 characters, 13/13 ground-truth probes matched ([proof](real-content-image-pdf-ocr-0651.md))), but that is a clean rasterization, not a paper scan: skew, speckle, fax artifacts, handwriting, stamps, rotation and low contrast remain untested, as do multi-page OCR accuracy and table fidelity. A separate **real** 258-page text packet exercise reported `Read 12 of 258 pages; completed pages retained. Retry to continue.` with 12 durable checkpoints and 18,080 retained characters ([proof](real-council-packet-ingestion-0651.md)); its resume step was not re-run to completion, so the skip-already-read behaviour rests on `story-documents.test.ts` (7/7 with the 37-page bounded-retry fixture). The bounded-retry fix is proven by the red-vs-green regression, not yet by a live multi-batch provider retry |
| 7. Story Workspace evidence-check finished | **Verified live** | Targeted ingestion/reconciliation tests remain green. On 2026-09-17, isolated story/4 uploaded a 100-byte meeting record, drafted with Codex Luna, and ran the check against that document: the check completed, validated the passage, loaded a checked version with 1 proposed change, and preserved Keep/Restore/Publish as separate editor decisions. No console errors; see [the live evidence-check proof](story-evidence-check-live-0651.md) | One 100-byte text document and one provider; does not prove PDF/OCR, multi-document, contradiction, or full factual coverage |
| 8. Dark Desk useful as a lead-finding desk; ≥5 bounded runs and the September 3-8 replay | **Eight bounded live runs, including the replay, all recorded** | `0506f38`, `7da28c8`, `2d36449`, `b626498`, `192d66b`, `917dcad`, `b5b71e4`, `5ff51b0` plus `docs/proofs/dark-desk-run-budget-0651.md`. Runs 3-8 produced 3, 6, 3, 4, 3 and 4 signals with confidences and explicit gaps; runs 3, 6, and 7 ended **`hop-limit` by design**; run 7 was the first round whose editor brief ran; run 8 ended on the clock with 4 signals and confirmed live verification provider labels (`codex-luna` / `gpt-5.6-luna`) | One newsroom and one model (`codex-luna`), so no cross-model claim. Signals are leads, not findings. The reserve is a check before a call, not a cap on it; run 8's brief timed out after a selector crossed the reserve |
| 9. Pull follow-up behaviour + one bounded retrieval flow | Verified live | `4d49359` plus `docs/proofs/pull-bounded-flow-0651.md`. `scripts/pull-progress-e2e.mjs`: mechanical-stage copy visible, counters live, job reattached after reload, Stop→Continue resumed without loss, `1 searches · 1 providers · 6 index pages · 4 documents opened · 4 saved`, completed state survived a second reload | One flow and one query. Durability was proven across browser reloads inside one server process, **not across a server restart** (the run used an in-memory database) |
| 10. Public and Editor's Desk routes coherent; correction email | Routes verified; correction wiring verified on the live site | `d1c6833` route smoke and the 23-step desk walk. The reported authenticated-`/desk` defect (desk click renders the public homepage) was **not reproduced on the isolated candidate** in any flow: hard load of `/desk`, home → Editor's desk click, footer link, trailing slash, mobile viewport, and desk→public→home→click all rendered the desk shell and desk home with the public hero absent and zero console/request errors. The candidate's desk routing files (`desk.tsx`, `desk.index.tsx`, `routeTree.gen.ts`, `desk-chrome.tsx`) are byte-identical to production's merge `c432737`, and production's deployed `.output` postdates its HEAD commit, so the production observation remains unexplained and **unverified live**. A new durable guard `scripts/desk-route-regression.mjs` (added in `3ba690f`) runs both required cases against a signed-in candidate and fails if `/desk` renders the public homepage; it passes (`{"ok":true,"cases":2}`). Original route evidence: live `https://townreporter.org/corrections` read: form present, `mailto:townreporter@gmail.com`, submit control present, no not-configured fallback, page states it has not sent. Production `/sitemap.xml` returned 200 with 58 article URLs, and the first article returned 200 with its `<h1>` and ~5,132 characters of body text. Isolated routes `/desk/scan`, `/desk/published`, `/desk/follow-ups`, and `/desk/stats` returned 200 with their own headings; `/desk/memory` redirects to `/desk/published` by design. The correction submission flow built a configured-editor `mailto:` with the headline, correction text, and explicit Not provided lines; see [the corrections proof](corrections-live-0651.md) | **Email delivery cannot be proven**; a `mailto:` hands off to the reader's own mail client. No exhaustive whole-site walkthrough is claimed. The reported authenticated-`/desk` public-homepage render was **not reproduced on the candidate**; if it still occurs on production it needs a live re-check with `scripts/desk-route-regression.mjs` against a signed-in production session |
| 11. Documentation and operator help aligned with reality | Aligned where behavior changed; live docs checked for unsupported claims and broken links | `e0adfa9` aligned Dark Desk run-meter and verification-count behavior; `20c14ee` Windows documentation; dated proofs now cover routes, first run, Pull, corrections, Dark Desk runs, Story evidence-check, image-only OCR, and the second provider. `scripts/docs-links.test.mjs` currently resolves **50 live markdown files, 229 relative links, 0 missing**; anchors 34, 0 broken; route references 28, 0 unresolved. The docs audit also corrected a phantom route, the job-kind count, stale Dark Desk acceptance text, the scheduled-task count, and the final report's old Queue-control claim | The live-document read-through is now complete: `README.md`, `docs/manual.md` and `docs/editor.md` were read end to end, plus the twelve other live guides, with each checked claim matched against source. It does not cover archived release notes (`CHANGELOG.md`, `docs/HALO-UPDATE-*.md`, `docs/releases/0.6.*.md`) or the dated proof documents |

## Summary of what is proven and what is not

**Proven with live or executed evidence:** the promotion guard's two cases; first-run and route behavior; Queue redraft state; PDF checkpoint resume; registry consistency; eight bounded Dark Desk runs including the replay; one bounded Pull retrieval; live correction wiring; the live Story evidence-check against an uploaded document; live image-only PDF OCR ingestion; live ingestion of a real 258-page published council packet with honest partial-read reporting and durable page checkpoints; the bounded-retry repair proven red-vs-green on a 37-page fixture (one retry = one 12-page batch plus a short final remainder); live vision OCR of real council-packet page images matching the authoritative text layer; and one live named Claude Sonnet draft.

**Not proven, and not claimed:** the authenticated-`/desk` public-homepage defect on the isolated candidate (not reproduced in any flow; production still unverified live with the new guard); fresh-machine human acceptance of the Windows install; OCR quality on degraded *paper* scans (the real-content OCR exercise used a clean rasterization, and the real 258-page packet has a selectable text layer); email delivery. The live documentation was read end to end against the current product, but archived release notes and dated proof documents were not re-audited.

**Authorization:** no publish, merge, tag, release, or deployment from this task. That requires Scott's explicit instruction after he has this report.
