# TownReporter — TODO (canonical, in-repo)

## Current release work — 2026-09-25

The current software version is [0.6.65](docs/releases/0.6.65.md): the Control page. The Desktop shortcut opens a local page on `127.0.0.1:3095` instead of a numbered terminal menu: one line reads **Everything is up** or **N things need attention**, each check is a card with a plain verdict and the one button that fixes it, and the six buttons carry the menu's own wording. It reports what `ops/status.ps1 -Json` says and changes nothing until a button is pressed; it answers only on loopback, refuses any button press that is not the page's own, runs one action at a time, streams the action's output into the page, and asks in a dialog before it takes the paper offline. Nothing on the page publishes, deletes or edits content. The numbered menu is still the fallback, with a seventh entry that opens the page. The release guide keeps source, package, GitHub publication, and production deployment as separate facts.

This change adds no migration and no scheduled task: the page is a reader and six fixed actions over the scripts that already exist, so the existing logon task and five-minute watchdog are unchanged.

The following acceptance work remains EXPLICITLY OPEN and is not closed by this release: fresh-machine human acceptance of the Windows install; the live provider matrix (configured-provider behaviour across the full provider set); five useful Dark Desk outcomes plus the September 3–8 replay; and the post-crash detached-child limitation. Production deployment and live-model proof are not asserted here. Nothing in this release was run against a real AI model, and no screenshot here was taken against the live paper.

Meeting capture specifically does not yet include: a provisional-to-revision cycle observed against a real capture (the re-check runs, but nothing inserts a link row, so it finds nothing to look at), and any path from a transcript to a story. Section 5 produces chunks, alignments and structured votes and deliberately does not draft.

Tag boundaries: `v0.6.65` is the expected tag for this software version; its existence and target are verified from GitHub rather than predicted in this file. `v0.6.64`, the previous version's expected tag, is verified from GitHub the same way and is not asserted here. `v0.6.60` remains published at `e97db885364a09db6914371e53a7c3a961cd9c23`; `v0.6.56` remains published at `86b66e1b5310730a01a2f1e6f35eb3d993afed42`; `v0.6.55` remains published at peeled commit `ce9db0344afc8dff63f57aceed763d9b0fe7864b`; `v0.6.54` remains published at `c9402d794552556519710c8ee347d6d581a91c6a`; `v0.6.53` at `baa6566`; `v0.6.52` at `9834e81`.
