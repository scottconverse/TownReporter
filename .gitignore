node_modules/
.project_id
.github_repo
.env
.env.*
!.env.example
logs/
.output/
.vercel/
.tanstack/
.node_modules.lock
attachments/

# Transient capture and build output. An audit found 150 screenshots (11 MB)
# and 25 browser captures plus five duplicate source archives (3.9 MB) tracked
# here (ENG-010). Clones pay for those forever, and a committed source archive
# can be mistaken for canonical code.
#
# docs/images/ stays tracked on purpose: eleven curated manual screenshots that
# the documentation links to. That is the deliberate exception.
screenshots/
artifacts/browsed_files/
artifacts/imagine_images/
artifacts/*.zip
artifacts/*.tar.gz
artifacts/*.bin
artifacts/*.html
artifacts/RENAME-TO-ZIP.txt

# Audit evidence: the written reports are the deliverable and stay tracked.
# The screenshots and capture scripts behind them are reproducible output,
# and 29 MB of them briefly went in here -- which is the exact defect
# ENG-010 was about. Reports in, evidence out.
artifacts/audit-*/uiux-screens/
artifacts/audit-*/qa-evidence/
