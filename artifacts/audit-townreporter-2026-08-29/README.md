# Frozen audit record — 2026-08-29

This directory is a historical audit snapshot. It is preserved as evidence of
what the audit examined and proposed on 2026-08-29; it is not current product,
installation, operator, or developer guidance.

The snapshot contains twelve relative Markdown links that were either example
placeholders or paths written for the temporary rewrite tree captured here.
Those links are intentionally not repaired: changing them would rewrite the
historical evidence. Current documentation link checks explicitly cover the
live manuals under `docs/` plus the repository's current root guides, and do
not treat this frozen snapshot as navigable documentation.

Use the current `README.md`, `SELF-HOSTING.md`, `TODO.md`, and `docs/` tree for
instructions and product claims.
